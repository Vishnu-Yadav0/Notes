We need to set java binary path in environment PATH variable to use java binaries. So that you can simply run java instead of the full path.

Once done, verify that you can invoke java simply by running java command.

Set path variable with the command:
 export PATH=$PATH:/opt/jdk-20/bin

Install the Apache Ant build tool 
sudo yum install -y ant

Check the ANT build configuration file /opt/ant/build.xml

If you run the ant jar command, where will the target jar file be stored? 
  ==>/opt/ant/dist/

thor@host01 /opt/ant$ ant jar
Buildfile: /opt/ant/build.xml

compile:
    [javac] /opt/ant/build.xml:5: warning: 'includeantruntime' was not set, defaulting to build.sysclasspath=last; set to false for repeatable builds

jar:
      [jar] Building jar: /opt/ant/dist/MyClass.jar

BUILD SUCCESSFUL
Total time: 0 seconds


Compile and generate jar package using ant.  Run: ant compile jar 


Run ant to carry out all steps specified in the build configuration file /opt/ant/build.xml.

thor@host01 /opt/ant$ ant 
Buildfile: /opt/ant/build.xml

compile:
    [javac] /opt/ant/build.xml:5: warning: 'includeantruntime' was not set, defaulting to build.sysclasspath=last; set to false for repeatable builds

jar:

docs:
  [javadoc] Generating Javadoc
  [javadoc] Javadoc execution
  [javadoc] Loading source file /opt/ant/src/MyClass.java...
  [javadoc] Constructing Javadoc information...
  [javadoc] Standard Doclet version 11.0.20.1
  [javadoc] Building tree for all the packages and classes...
  [javadoc] Building index for all the packages and classes...
  [javadoc] Building index for all classes...
  [javadoc] Building index for all classes...

run:
     [java] Hello Kodekloud

main:

BUILD SUCCESSFUL
Total time: 3 seconds


Install the maven build automation tool
Run: sudo yum install -y maven to install maven.

Run: cd /home/thor/; npm install file to install NodeJS file package.

Run: sudo npm install file -g to install file module globally.

-------------------------------------------------------------------------
 sudo iptables -L --> to list out all rules in the server

---------------------------------------------------------------------------

On devapp01, add an incoming rule permitting SSH and HTTP connection from Bob's Laptop.
Bob's Laptop has an IP address of 172.16.238.187.

Run: sudo iptables -A INPUT  -p TCP -s 172.16.238.187 --dport 22 -j ACCEPT and sudo iptables -A INPUT  -p TCP -s 172.16.238.187 --dport 80 -j ACCEPT

---------------------------------------------------------------------------

Now, lockdown incoming traffic on devapp01. Drop incoming connections from any source on any destination port for any protocol (TCP/UDP).


Remember, this rule should be at the bottom of the chain for the SSH and HTTP access from caleston-lp10 to work.

Run: sudo iptables -A INPUT -j DROP

---------------------------------------------------------------------------
Change the SHELL for Bob from bash to Bourne Shell
Run:chsh


For example, to add a variable using the command line:

echo 'export MY_VARIABLE="example_value"' >> ~/.profile

This command appends the export statement to the end of the ~/.profile file, making the variable MY_VARIABLE persistent across sessions.

To add an alias, like ll for ls -l, use:

echo 'alias ll="ls -l"' >> ~/.profile

---------------------------------------------------------------------------
Update Bob's prompt so that it displays the date as per the format below:


Example: [Wed Apr 22]bob@caleston-lp10:~$
Make sure the change is made persistent.


Run:PS1='[\d]\u@\h:\w$' and add this to the ~/.profile file echo 'PS1="[\d]\u@\h:\w$"' >> ~/.profile


What is the exact version of kernel running in this system?
RUN: uname -r

What is the Kernel Version in 4.15.0-88-generic?
Hint: first digit 
Ans: 4

What is the major version number of the kernel 4.15.0-88-generic?
Look for the second digit after the kernel version separated by a dot(.). In this case, it is 15.


What is the command to print the messages generated by the kernel?
dmesg


What is the default systemd target set in this system?
sudo systemctl get-default

Now, change the target to multi-user.target
sudo systemctl set-default multi-user.target


Read the file /usr/share/man/man1/tail.1.gz and without extracting redirect the contents to a file called /home/bob/pipes.

Run the command: zcat /usr/share/man/man1/tail.1.gz | tee /home/bob/pipes


What is the IP address of the DNS Server used in this system?
run: cat /etc/resolv.conf and look at the server's IP address

/etc/resolv.conf is the configuration file used for the DNS Server



___________________________________________________________________________

			Jenkins Installation in CentOS 
________________________________________________________________________

sudo yum install epel-release -y
sudo yum install java-11-openjdk -y
sudo wget -O /etc/yum.repos.d/jenkins.repo https://pkg.jenkins.io/redhat-stable/jenkins.repo --no-check-certificate
sudo rpm --import http://pkg.jenkins.io/redhat-stable/jenkins.io-2023.key
sudo yum install jenkins -y

if you want to change the default port of Jenkins then:
 Edit /lib/systemd/system/jenkins.service file and change Jenkins port to 8090 by updating Environment="JENKINS_PORT=" variable value.


It should look like this:

Environment="JENKINS_PORT=8090"


Once done start Jenkins service.

sudo systemctl start jenkins



_____________________________________________



